services:
  elasticsearch:
    volumes:
      - ./certs:/usr/share/elasticsearch/config/certs:ro
    environment:
      # --- HTTP TLS (9200) ---
      - xpack.security.http.ssl.enabled=true
      - xpack.security.http.ssl.key=certs/elasticsearch/elasticsearch.key
      - xpack.security.http.ssl.certificate=certs/elasticsearch/elasticsearch.crt
      - xpack.security.http.ssl.certificate_authorities=certs/ca/ca.crt

      # --- Transport TLS (9300) ---
      - xpack.security.transport.ssl.enabled=true
      - xpack.security.transport.ssl.verification_mode=certificate
      - xpack.security.transport.ssl.key=certs/elasticsearch/elasticsearch.key
      - xpack.security.transport.ssl.certificate=certs/elasticsearch/elasticsearch.crt
      - xpack.security.transport.ssl.certificate_authorities=certs/ca/ca.crt

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -s --cacert /usr/share/elasticsearch/config/certs/ca/ca.crt -u elastic:${ELASTIC_PASSWORD} https://localhost:9200 >/dev/null || exit 1",
        ]

  kibana:
    volumes:
      - ./config/kibana/kibana.tls.yml:/usr/share/kibana/config/kibana.yml:ro
      - ./certs:/usr/share/kibana/config/certs:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'curl -k -s https://localhost:5601/api/status | grep -q ''"level":"available"''',
        ]
      interval: 10s
      timeout: 5s
      retries: 60
